---
layout: layouts/home.njk
tags:
  - nav
navtitle: "Blog"
date: 2020-01-01
---
{% set postslist = collections.posts %}

<!-- <p>Look behind you, a three-headed monkey.</p> -->

<div class="about">
  <img src="/img/dithered-avatar.png" alt="" style="">
  <p>Hey there, I'm Vlada.</p> 

  <p>I'm currently working on conceptualization and realization of fintech solutions at <a class="purple-text font-extra-bold" href="http://codereflect.io" target="_blank">Reflect</a>.</p>
    
  <p>Previously, I worked as a developer in the US healthcare industry, where I was involved in the creation of HIPAA-compliant environments and the development of applications for startups and enterprises.</p>

  <p>If you have any comments, questions, or suggestions, please send them to my personal <a class="purple-text font-extra-bold" href="mailto:svlada@gmail.com" target="_blank">email</a> address. To get new posts, please use my <a class="purple-text font-extra-bold" href="{{ metadata.feed.path | url }}" type="application/atom+xml">RSS feed</a>.</p>
</div>

<h2>Posts</h2>

<p>Sometimes, I write.</p>

<ul class="postlist">
  {% for post in postslist | reverse %}
    {% if post.data.title != "draft" %}
    <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
      <a href="{{ post.url | url }}" class="postlist-link purple-text"> {% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a> <i class="small-text">{{ post.data.date | readableDate() }}</i>
    </li>
    {% endif %}
  {% endfor %}
</ul>

{% set lablist = collections.lab %}

<h2>Sketchbook</h2>

<p>Experiments and playthings.</p>

<ul class="postlist">
  {% for lab in lablist | reverse %}
    {% if lab.data.title != "draft" %}
    <li class="postlist-item{% if lab.url == url %} postlist-item-active{% endif %}">
      <a href="{{ lab.url | url }}" class="postlist-link purple-text"> {% if lab.data.title %}{{ lab.data.title }}{% else %}<code>{{ lab.url }}</code>{% endif %}</a> <i class="small-text">{{ lab.data.date | readableDate() }}</i>
    </li>
    {% endif %}
  {% endfor %}
</ul>