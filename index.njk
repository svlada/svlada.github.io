---
layout: layouts/home.njk
tags:
  - nav
navtitle: "Blog"
date: 2020-01-01
---
{% set postslist = collections.posts %}

<!-- <p>Look behind you, a three-headed monkey.</p> -->
<p>Hi! </p>

<p>Presently engaged in the conceptualization and realization of fintech solutions at <a class="purple-text font-extra-bold" href="http://codereflect.io" target="_blank">Reflect</a>.</p>
  
<p>Previously, I worked as a developer in the healthcare industry, where I was involved in the creation of HIPAA-compliant environments and the development of applications.</p>
{% set postslist = collections.posts %}

<p>If you have any comments, questions, or suggestions, please send them to my personal <a class="purple-text font-extra-bold" href="mailto:svlada@gmail.com" target="_blank">email</a> address. To get new posts, please use my <a class="purple-text font-extra-bold" href="{{ metadata.feed.path | url }}" type="application/atom+xml">RSS feed</a>.</p>
<ul class="postlist">
  {% for post in postslist | reverse %}
    {% if post.data.title != "draft" %}
    <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}">
      <a href="{{ post.url | url }}" class="postlist-link purple-text"> {% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a> {{ post.data.date | readableDate() }}
    </li>
    {% endif %}
  {% endfor %}
</ul>
